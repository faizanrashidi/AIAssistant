<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ai.assistant">

    <!-- Permissions -->
    <uses-permission android:permission="android.permission.CAMERA" />
    <uses-permission android:permission="android.permission.RECORD_AUDIO" />
    <uses-permission android:permission="android.permission.CALL_PHONE" />
    <uses-permission android:permission="android.permission.READ_PHONE_STATE" />
    <uses-permission android:permission="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:permission="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:permission="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:permission="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:permission="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:permission="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AI Voice Assistant"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground Service -->
        <service
            android:name=".service.CoreForegroundService"
            android:enabled="true"
            android:exported="false" />

        <!-- Notification Listener Service -->
        <service
            android:name=".service.NotificationReaderService"
            android:label="AI Notification Reader"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Geofence Broadcast Receiver -->
        <receiver
            android:name=".receiver.GeofenceBroadcastReceiver"
            android:enabled="true"
            android:exported="true" />

    </application>

</manifest>
